---
title: 系统安装
description: 使用 deb、rpm、apk 安装包部署 li-gh-proxy，以及服务管理命令。
---

## 安装脚本

```bash
curl -fsSL https://raw.githubusercontent.com/LiStudioorg/li-gh-proxy/main/install.sh | sh
```

脚本会自动识别 `amd64` / `arm64` 与 `apt`、`dnf`、`apk` 等包管理器。

## 文件路径

| 路径 | 说明 |
|------|------|
| `/etc/li-gh-proxy/config.toml` | 配置文件 |
| `/usr/bin/li-gh-proxy` | 二进制文件 |
| `/lib/systemd/system/li-gh-proxy.service` | systemd 服务（Debian/Ubuntu/RHEL 等） |
| `/etc/init.d/li-gh-proxy` | OpenRC 服务（Alpine） |
| `/var/log/li-gh-proxy.log` | Alpine 日志文件 |

## systemd 管理

```bash
sudo systemctl status li-gh-proxy    # 查看运行状态
sudo systemctl restart li-gh-proxy   # 修改配置后重启
sudo journalctl -u li-gh-proxy -f    # 实时查看日志
sudo nano /etc/li-gh-proxy/config.toml  # 编辑配置
```

卸载：

```bash
sudo apt purge li-gh-proxy   # Debian/Ubuntu 卸载并清除配置
```

## OpenRC 管理（Alpine）

```bash
sudo rc-service li-gh-proxy status   # 查看状态
sudo rc-service li-gh-proxy restart  # 重启服务
sudo tail -f /var/log/li-gh-proxy.log  # 查看日志
sudo vi /etc/li-gh-proxy/config.toml   # 编辑配置
```

卸载：

```bash
sudo apk del li-gh-proxy   # 删除安装包
```

## 生产环境建议

公网或团队使用时，建议为 li-gh-proxy 绑定**自有域名**，通过 Caddy / Nginx 等反向代理并**开启 HTTPS**，而不是长期直接暴露 `http://IP:5000`。这样可同时获得 TLS、正确的客户端 IP 传递与更安全的访问控制。配置示例见 [反向代理](/deployment/reverse-proxy/) 与 [推荐部署架构](/deployment/architecture/)。
